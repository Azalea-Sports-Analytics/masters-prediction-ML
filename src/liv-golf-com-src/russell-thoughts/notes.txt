

postman for chrome

https://chromewebstore.google.com/detail/postman-interceptor/aicmkgpgakddgnaphhhpliifpcfhicfo



https://chatgpt.com/share/67bb8195-0c08-800e-9c6f-7c6c78db9ebd




Some of the .json files aren't quite json.



ChatGPT says:

This format appears to be a custom serialized data structure where:

Numbers followed by : are keys

Values can be either objects (in {}) or arrays (in [])

References to other objects are made using $ followed by the key (e.g., "$4" references the object at key 4:)

This is likely a space-efficient way to store the golf tournament data while maintaining relationships between different pieces of information, similar to a normalized database structure.


---


in branch `rlb`

directory `src/liv-golf-com-src/russell-thoughts`

there is the first part of a conversion program for the "not quite json files"

`convert_near_jason.py`

that detect ranges that I don't know what to do with

the original sample I had grabbed was

`text-leader.txt`

which gives one bad range at the end

`bad-ranges.txt`

What do we want to do with bad ranges?

Options include

* refuse to process the file
* ignore the bad ranges completely
* give a warning and skip the bad ranges
* put the bad ranges into single file with some separator between them
* put the bad ranges into separate files in a directory of bad ranges